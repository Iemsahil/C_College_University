#include<stdio.h>

int main()
{
    int x = 3;
    int y = 3;

    printf("The value of 3*x + 4*y is %d\n ",3*x + 4*y);
    printf("The value of 8*x / 3*y is %d\n",8*x / 3*y);
    // 8*3/3*3= 24/9 is wrong answer
    // 24/3*3
    // 8*3
    // 24

    return 0;
}